# CafeERP Cloudflare Test

This directory is intentionally only a marker for the Cloudflare staging branch. Production remains on the main branch.

Cloudflare Workers should use the current automatic Next.js/vinext configuration rather than modifying the production Next.js configuration until compatibility testing is complete.
