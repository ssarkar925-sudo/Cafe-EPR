-- Remove the legacy create_business_txn overload. Its defaulted arguments make
-- RPC calls ambiguous with the newer overload that accepts p_portal_charge.
drop function if exists public.create_business_txn(
  text,
  date,
  timestamp with time zone,
  uuid,
  text,
  text,
  text,
  text,
  uuid,
  uuid,
  uuid,
  text,
  text,
  text,
  text,
  text,
  text,
  text,
  text,
  text,
  text,
  numeric,
  numeric,
  numeric,
  text,
  text,
  text,
  uuid,
  text,
  text
);

-- Keep the canonical 30-argument create_business_txn overload as the only
-- callable version, including p_portal_charge with a safe default of 0.
revoke execute on function public.create_business_txn(
  text,
  date,
  timestamp with time zone,
  uuid,
  text,
  text,
  text,
  text,
  uuid,
  uuid,
  uuid,
  text,
  text,
  text,
  text,
  text,
  text,
  text,
  text,
  text,
  numeric,
  numeric,
  numeric,
  text,
  text,
  text,
  uuid,
  text,
  text,
  numeric
) from anon;
