// OpenNext Cloudflare configuration for CafeERP.
// Kept as the minimal supported configuration for the Cloudflare adapter.
// Deployment marker: force Workers Builds to consume the current main revision.
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig();
